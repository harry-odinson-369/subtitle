name: subtitle
description: ->
  A library that makes it easy to work with multiple subtitle/caption file formats,
  written with highly efficient code, highly customizable (90%), supports Null Safety.
version: 0.2.0
publish_to: none

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  path: ^1.9.0
  universal_io: ^2.2.2

dev_dependencies:
  lints: ^2.1.1
  test: ^1.24.6
